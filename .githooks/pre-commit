#!/bin/sh
echo "🔍 pre-commit: lint + unit tests…"
if ! ./gradlew ktlintCheck test --console=plain; then
    echo ""
    echo "❌ pre-commit failed — lint issues? run 'just format'. test failures? see above."
    exit 1
fi
